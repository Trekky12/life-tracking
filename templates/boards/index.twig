{% extends "base.twig" %}

{% set active = 'boards' %}

{% block pageheader %}
	<div class="page-header">
		<div class="page-headline">
			<h2>{{"BOARDS"|trans}}</h2>
		</div>
		<div class="buttons">
			<a href="{{ url_for('boards_edit') }}">
				<button class="button button-add-item">{{ fontawesome('fas fa-plus') }}
					{{"ADD"|trans}}</button>
			</a>
		</div>
	</div>
{% endblock %}

{% block body %}
	<div class="inner-content">
		<div class="page-content">
			<form method="GET" id="tableHeaderFilter" class="search-filter">
				<div class="filter-row">
					<div class="filter-group">
						<div class="filter-element">
							<div class="select-wrapper form-control">
								<select name="archive">
									<option value="0" {% if archive == 0 %} selected {% endif %}>{{"SHOW_NOT_ARCHIVED"|trans}}</option>
									<option value="1" {% if archive == 1 %} selected {% endif %}>{{"SHOW_ARCHIVED"|trans}}</option>
									<option value="" {% if archive is empty %} selected {% endif %}>{{"SHOW_ALL"|trans}}</option>
								</select>
							</div>
						</div>
					</div>
				</div>
				<div class="filter-row">
					<div class="filter-element">
						<input type="submit" class="button" value="{{"FILTER_RESULT"|trans}}"/>
					</div>
				</div>
			</form>
		</div>
	</div>

	<div class="inner-content">
		<div class="page-content">
			<table id="boards_table" class="table table-hover small">
				<thead>
					<tr>
						<th>{{"NAME"|trans}}</th>
						<th></th>
						<th></th>
					</tr>
				</thead>
				<tbody>
					{% for board in boards %}
						<tr>
							<td>
								<a href="{{ url_for('boards_view', {'hash' : board.hash}) }}">{{board.name|raw}}</a>
							</td>
							<td>
								{% if board.user == user.id %}
									<a href="{{ url_for('boards_edit', {'id' : board.id}) }}">{{ fontawesome('fas fa-pen-to-square') }}</a>
								{% endif %}
							</td>
							<td>
								{% if board.user == user.id %}
									<a href="#" data-url="{{ url_for('boards_delete', {'id' : board.id}) }}" class="btn-delete" data-confirm="{{ "REALLY_DELETE_BOARD"|trans}}">{{ fontawesome('fas fa-trash') }}</a>
								{% endif %}
							</td>
						</tr>
					{% endfor %}
				</tbody>
			</table>
		</div>
	</div>

{% endblock %}
