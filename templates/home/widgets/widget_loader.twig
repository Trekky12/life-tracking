<div class="request_widget" data-id="{{ widget["id"] }}" data-reload="{{ widget["reload"] }}" data-widget="{{ widget["name"] }}" data-options="{{ widget["options"] | json_encode() }}">
	<span class="loading-icon centered spinning">{{ fontawesome('fas fa-circle-notch') }}</span>
</div>
