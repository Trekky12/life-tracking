{% extends "base.twig" %}

{% set active = 'profile' %}

{% block body %}

    <div class="inner-content">
        <div class="page-header"><h2>{{lang["MOBILE_FAVORITES"]}} </h2> <a href="{{  path_for('users_mobile_favorites_edit') }}"><button class="white"><i class="fa fa-plus" aria-hidden="true"></i> {{lang["ADD"]}}</button></a></div>
        <div class="page-content">
            <table id="mobile_favorites_table" class="table table-hover small">
                <thead>
                    <tr>
                        <th>{{ lang["POSITION"] }}</th>
                        <th>{{ lang["ICON"] }}</th>
                        <th>{{ lang["URL"] }}</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for el in list %}
                        <tr>
                            <td data-title="{{lang["POSITION"]}}">{{ el.position }}</td> 
                            <td data-title="{{lang["ICON"]}}"><i class="fa fa-lg {{el.icon}}"></i></td> 
                            <td data-title="{{lang["URL"]}}">{{el.url|raw}}</td> 
                            <td><a href="{{  path_for('users_mobile_favorites_edit', {'id' : el.id}) }}"><span class="fa fa-pencil-square-o fa-lg"></span></a></td>
                            <td><a href="#" data-url="{{  path_for('users_mobile_favorites_delete', {'id' : el.id}) }}" class="btn-delete"><span class="fa fa-trash fa-lg"></span></a></td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}