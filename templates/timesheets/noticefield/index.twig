{% extends "base.twig" %}

{% set active = 'timesheets' %}

{% block body %}

    <div class="inner-content">
        <div class="page-header"><h2>{{lang["TIMESHEETS_NOTICEFIELDS"]}} | {{ project.name }}</h2> 
            <div class="buttons">
                <a href="{{ url_for_with_last_query_params('timesheets')}}"><button class="button">{{lang["BACK"]}} {{ fontawesome('fas fa-turn-up') }}</button></a>
                <a href="{{  url_for('timesheets_noticefields_edit', {'project': project.hash}) }}"><button class="button">{{ fontawesome('fas fa-plus') }} {{lang["ADD"]}}</button></a>
            </div>
        </div>
        <div class="page-content">
            <table id="noticefields_table" class="table table-hover small">
                <thead>
                    <tr>
                        <th>{{lang["NAME"]}}</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for field in fields %}
                        <tr>
                            <td>{{field.name}}</td> 
                            <td>
                                <a href="{{  url_for('timesheets_noticefields_edit', {'id' : field.id, 'project' : project.hash}) }}">{{ fontawesome('fas fa-pen-to-square') }}</a>
                            </td>
                            <td>
                                <a href="#" data-url="{{  url_for('timesheets_noticefields_delete', {'id' : field.id, 'project' : project.hash}) }}" class="btn-delete">{{ fontawesome('fas fa-trash') }}</a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}