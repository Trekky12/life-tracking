{% extends "base.twig" %}

{% set active = 'timesheets' %}

{% block style %}
    <link rel="stylesheet" type="text/css" href="{{baseURL}}/static/assets/css/selectr.min.css" />
{% endblock %}

{% block javascript %}
    <script src="{{baseURL}}/static/js/datefilter.js?ver=20191031"></script>
    <script src="{{baseURL}}/static/assets/js/selectr.min.js"></script>
    <script src="{{baseURL}}/static/js/timesheets.js?ver=20210514"></script>
{% endblock %}

{% block body %}


    <div class="inner-content">
        <div class="page-header"><h2>{{lang["TIMESHEETS"]}} | {{project.name|raw}}</h2> 
            <div class="buttons">
                <a href="{{  url_for('timesheets_sheets', {'project' : project.hash}) }}"><button class="white">{{lang["BACK"]}} {{ fontawesome('fas fa-level-up-alt') }}</button></a>
            </div>
        </div>
        <div class="page-content">

            <form method="GET" action="{{  url_for('timesheets_sheets_export', {'project' : project.hash}) }}">

                <div class="filter-content form-group">
                    <div class="d1ate-element">
                        <input type="text" id="dateRange" value="{{from}} to {{to}}" />

                        <input type="hidden" id="inputStart" name="from" value="{{from}}"/>
                        <input type="hidden" id="inputEnd" name="to" value="{{to}}"/>

                    </div>
                </div>

                <div class="categories-filter form-group">
                    <select id="category-filter" multiple name="categories[]" class="category">
                        {% for cat in categories %}
                            <option value="{{ cat.id }}" {% if cat.id in categories_selected %}selected{% endif %}>{{ cat.name }}</option>
                        {% endfor %}
                    </select>
                </div>
                <div class="form-group">
                    <div class="left">
                        <label class="radio" for="radioExcel">
                            <input name="type" type="radio" value="excel" id="radioExcel" checked>
                            {{lang["EXPORT_EXCEL"]}}
                        </label>                               
                    </div>
                    <div class="right">
                        <label class="radio" for="radioWord">
                            <input name="type" type="radio" value="word" id="radioWord">
                            {{lang["EXPORT_WORD"]}}
                        </label>
                    </div>
                </div>

                <div class="form-group">
                    <input type="submit" class="no-loading" value="{{lang["TIMESHEETS_EXPORT"]}}"/>
                </div>
            </form>  
        </div>
    </div>


{% endblock %}        
