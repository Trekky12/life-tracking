{% extends "base.twig" %}

{% set active = 'workouts' %}

{% block body %}

    <div class="inner-content">
        <div class="page-header"><h2>{{lang["WORKOUTS_BODYPARTS"]}}</h2> 
            <div class="buttons">
                <a href="{{  url_for('workouts_bodyparts_edit') }}"><button class="button">{{ fontawesome('fas fa-plus') }} {{lang["ADD"]}}</button></a>
            </div>
        </div>
        <div class="page-content">
            <table id="workouts_bodyparts_table" class="table table-hover small">
                <thead>
                    <tr>
                        <th>{{lang["NAME"]}}</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for bodypart in bodyparts %}
                        <tr>
                            <td>{{bodypart.name|raw}}</td> 
                            <td><a href="{{  url_for('workouts_bodyparts_edit', {'id' : bodypart.id}) }}">{{ fontawesome('fas fa-pen-to-square') }}</a></td>
                            <td><a href="#" data-url="{{  url_for('workouts_bodyparts_delete', {'id' : bodypart.id}) }}" class="btn-delete">{{ fontawesome('fas fa-trash') }}</a></td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}