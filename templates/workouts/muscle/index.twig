{% extends "base.twig" %}

{% set active = 'workouts' %}

{% block body %}

    <div class="inner-content">
        <div class="page-header"><h2>{{lang["WORKOUTS_MUSCLES"]}}</h2> 
            <div class="buttons">
                <a href="{{  url_for('workouts_muscles_image') }}"><button class="button">{{lang["IMAGE"]}}</button></a>
                <a href="{{  url_for('workouts_muscles_edit') }}"><button class="button">{{ fontawesome('fas fa-plus') }} {{lang["ADD"]}}</button></a>
            </div>
        </div>
        <div class="page-content">
            <table id="workouts_muscles_table" class="table table-hover small">
                <thead>
                    <tr>
                        <th>{{lang["NAME"]}}</th>
                        <th></th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for muscle in muscles %}
                        <tr>
                            <td>{{muscle.name|raw}}</td> 
                            <td><a href="{{  url_for('workouts_muscles_edit', {'id' : muscle.id}) }}">{{ fontawesome('fas fa-pen-to-square') }}</a></td>
                            <td><a href="#" data-url="{{  url_for('workouts_muscles_delete', {'id' : muscle.id}) }}" class="btn-delete">{{ fontawesome('fas fa-trash') }}</a></td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}